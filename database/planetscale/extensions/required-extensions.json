{
  "engine": "postgresql",
  "required": [
    {
      "name": "pgcrypto",
      "purpose": "cryptographic helpers and UUID generation fallback",
      "launchFeature": "identity and encrypted-field support"
    },
    {
      "name": "pg_trgm",
      "purpose": "fuzzy handle and public-search indexes",
      "launchFeature": "handle discovery"
    },
    {
      "name": "unaccent",
      "purpose": "accent-insensitive search normalization",
      "launchFeature": "public-search normalization"
    }
  ],
  "optional": [
    {
      "name": "postgis",
      "purpose": "deferred polygon and distance analysis",
      "launchFeature": "advanced geography",
      "availability": "blocked-by-provider",
      "launchBlocking": false
    }
  ],
  "failurePolicy": "Do not mark cryptographic, handle-search, or normalization capabilities ready until every required extension is installed and verified. PostGIS is explicitly deferred and is not a launch requirement.",
  "providerEvidence": {
    "checkedAt": "2026-07-28",
    "branch": "development",
    "status": "blocked",
    "reason": "pgcrypto, pg_trgm, and unaccent are installed on development; PostGIS is temporarily disabled by PlanetScale and excluded from the immutable extension allowlist."
  }
}
