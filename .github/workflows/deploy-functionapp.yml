name: Legacy Function deploy (blocked)

on:
  workflow_dispatch:
  workflow_call:

permissions:
  contents: read

jobs:
  blocked:
    runs-on: ubuntu-latest
    steps:
      - name: Block rebuild-and-deploy path
        run: |
          echo "::error::This legacy rebuild path is disabled. Use deploy-asora-function-mvp.yml with an exact validated SHA and CI artifact."
          exit 1
