# Native platform progress — 2026-07-27

## Completed

- Reconciled repository branch, remote branches, open PRs, versions, Cloudflare,
  and PlanetScale state.
- Created branch `codex/cloudflare-planetscale-provisioning` without discarding
  existing dirty-tree changes.
- Recorded data-disposition ADR and temporary shared-account scope manifest.
- Confirmed the current PlanetScale database is empty and safe from an
  application-record perspective; no destructive replacement was performed.
- Created synthetic development KV, four R2 buckets, six queues, and six DLQs
  with `lythaus-`/`-dev` names in the shared account.
- Added PG18 migration preflight, launch schema baseline, feature-flag seed,
  schema-scoped role grants, verification SQL, and restore SQL.
- Added UUIDv7 generation, cache-disabled Hyperdrive enforcement, presigned R2
  upload flow, outbox relay, duplicate-consumer inbox handling, and independent
  Access JWT verification.
- Added launch contract routes and explicit provider/feature-disabled responses.
- Preserved the legacy `/auth/email` OpenAPI path as explicitly deprecated and
  regenerated the bundled spec and Dart client.
- Routed privacy-request acceptance through the application transactional outbox;
  `lythaus-jobs` now persists the request with an idempotent privacy-role handler.
- Routed finalized media uploads to the media queue and implemented an Images-backed
  quarantine processor with magic-byte checks, dimension limits, WebP re-encoding,
  SHA-256 recording, approved-bucket publication, storage-ledger updates, and
  idempotent duplicate handling.
- Added deterministic account-delete and Data Passport export Workflows with
  legal-hold blocking, session revocation, content redaction, media purge,
  subject-locator reconciliation, private export manifests, and completion tombstones.
- Added configured email/password registration, verification, refresh-token
  rotation, logout, quota reservations, retention controls, consent-bounded
  geography, authenticated feed reads, follower fan-out, and declaration-conflict
  moderation handling. Added Google PKCE/OIDC linking and single-use password
  reset tokens. Provider secrets and delivery acceptance remain gated.
- Added account token-version revocation, administrator status controls, block,
  mute, bookmark, follow-removal, explicit custom-domain production validation,
  and an idempotent appeal lifecycle Workflow.
- Narrowed the root npm workspace to the native Workers and shared packages so
  unrelated marketing/control-panel dependencies are not pulled into native builds.
- Added Wrangler 4.114.0, generated Worker types, dry-run bundles, benchmark
  harness, migration validator, recovery runbook, and guarded workflows.
- Validation: native typecheck passed; native architecture tests passed 12/12;
  migration validator passed; existing Functions tests passed 208 suites,
  2,279 passed, 18 skipped.
- Exact-head native GitHub validation passed at `8d2035d4`: workers
  `30310667856`, migrations `30310669902`, secret scan `30310671819`.

## Blocked

- Dedicated Cloudflare account creation: API `1002 Forbidden: Account creation is not allowed`.
- PlanetScale development branch/role/replica provisioning: the authenticated
  MCP surface exposes read queries and metadata but no branch/database/role
  creation operation; the live database remains PostgreSQL 17.10, PS-5 ARM,
  Frankfurt, `main` only, zero application tables and zero replicas.
- Native Hyperdrive replacement bindings: require PlanetScale role credentials.
- Production account/zone/Workers Paid isolation, DNS, Turnstile, Access, and
  email sending: owner-side dedicated-account and DNS/billing actions remain.
- Cloudflare read access is restored and the shared-account inventory is verified;
  dedicated-account creation still returns API `1002 Forbidden`, so no production
  account mutation or Turnstile widget creation was attempted.
- Independent provider backup destination and restore evidence: no destination
  credentials were supplied or stored.
- Repository-wide CI remains non-green on the legacy dependency audit
  (`high=39`) and prior contract checks; the exact-head native checks pass
  remotely on `0460eb17` (`30310556528`, `30310558289`, `30310560298`).

## Gate matrix

| Gate | Status |
| --- | --- |
| 1 — Ownership and isolation | BLOCKED by dedicated account and data-disposition signature |
| 2 — Region and topology | BLOCKED by benchmark, PG18 reprovision, and HA replicas |
| 3 — Database and cryptographic governance | IN PROGRESS; code/evidence ready, live branch/roles pending |
| 4 — Runtime and product acceptance | IN PROGRESS; bundles dry-run, live deployment pending |
| 5 — Recovery and cutover | BLOCKED; no production data, domains, or independent restore |

## Exact next actions

1. Owner creates/transfers a dedicated Cloudflare account and supplies protected
   production account/zone identifiers.
2. Provision PlanetScale PostgreSQL 18 in benchmark regions/tiers, create
   `development`, and create schema-scoped roles with direct admin credentials.
3. Create four development Hyperdrive configurations with `verify-full`, then
   replace the placeholders in Wrangler development environments.
4. Login Wrangler with a scoped token, deploy all three development Workers,
   and execute the benchmark and end-to-end acceptance suite.
5. Obtain the signed data-disposition decision and independent backup target;
   only then progress the five gates.

## Latest native commits

- `8d2035d4`: native auth revocation/social controls, Turnstile and email
  provider boundaries, appeal lifecycle Workflow, endpoint-class benchmark,
  and production routing validator; all three remote native validation
  workflows passed at this exact head.
