# Beta Release Gate Evidence - 2026-06-17

Branch: `main`

## Runtime Provenance

- Main release merge: `6579b116707e3117a612ab42e8badd90ffc9b6f8`
- Evidence/deploy validation head: `fc4f3de6204aa610292d1e102677bd9dea09e5a9`
- Runtime delta after `6579b116`: smoke-gate tightening, persistent custom-domain CORS configuration, and evidence updates.
- Backend environment: Azure Functions `dev` at `https://asora-function-dev.azurewebsites.net/api`, also exposed through `https://dev.asora.co.za/api`.
- Frontend environment: Cloudflare Pages project `lythaus-web`, production branch `main`.
- Cloudflare Pages latest deployment: `fc4f3de6204aa610292d1e102677bd9dea09e5a9`, deployment `55b4616e-b8a8-4fd5-8376-39c9395ee9b2`, status `success`.

## Workflow Evidence

- Main CI: [27714658507](https://github.com/AsoraKK/Asora/actions/runs/27714658507), `success`
- Flutter CI: [27714659234](https://github.com/AsoraKK/Asora/actions/runs/27714659234), `success`
- API Contract: [27714659447](https://github.com/AsoraKK/Asora/actions/runs/27714659447), `success`
- Dev backend deploy: [27714660490](https://github.com/AsoraKK/Asora/actions/runs/27714660490), `success`
- Beta browser smoke from `main`: [27715205951](https://github.com/AsoraKK/Asora/actions/runs/27715205951), `success`

## Smoke Results

The `main` beta smoke ran against:

- Web: `https://lythaus-web.pages.dev`
- API: `https://asora-function-dev.azurewebsites.net/api`
- Admin API: `https://admin-api.asora.co.za`

The smoke report passed these release-gate checks:

- landing page and `/login` load
- `/auth/callback`, `/user/test`, and `/post/test` do not hard-404
- guest discovery feed works or cleanly empty-states
- no browser permission prompt was observed
- no forbidden `/api/api/...`, localhost, or private-IP requests were observed
- authenticated discover feed returns `200` with `Cache-Control: private, no-store`
- authenticated home feed returns `200` with `Cache-Control: private, no-store`
- authenticated following feed handles an empty state with `items: 0`, `count: 0`
- unauthorised admin API request is blocked by Cloudflare Access

Console/page evidence:

- `pageErrors`: none
- `failedRequests`: none
- console entries were limited to expected CSP report-only / unsafe-header noise plus the expected protected-resource `401`.

## CORS And Custom Domain

`OPTIONS https://dev.asora.co.za/api/feed/discover?limit=1`

Origin `https://lythaus-web.pages.dev`:

- `Access-Control-Allow-Origin: https://lythaus-web.pages.dev`
- `Access-Control-Allow-Methods: GET`
- `Vary: Authorization`

Origin `https://app.lythaus.asora.co.za`:

- DNS now resolves through Cloudflare.
- Azure platform CORS now returns `Access-Control-Allow-Origin: https://app.lythaus.asora.co.za`.
- Azure app setting `CORS_ALLOWED_ORIGINS` includes `https://app.lythaus.asora.co.za`.
- The dev deploy workflow now persists the custom-domain CORS origin on future deploys.
- Cloudflare Pages custom domain `app.lythaus.asora.co.za` exists on project `lythaus-web`, but certificate validation is still `pending`; HTTPS and beta smoke against the custom domain remain pending until the certificate becomes active.

## Admin Access

- Unauthorised admin request fails closed through Cloudflare Access.
- The `control-panel-to-admin-api` service token is now included in the Admin API Access app as a non-identity policy.
- A service-token request to `https://admin-api.asora.co.za/api/_admin/config` now reaches origin and returns an origin JSON `403` (`Token missing email claim`) instead of a Cloudflare Access redirect.
- Authorized admin read positive path still requires a human/admin session or admin JWT; do not mark that item fully complete from service-token-only evidence.

## Secrets / Rotation

- No secret rotation was performed in this pass.
- Cloudflare Access and smoke credentials were used only for verification and were not committed.
- No new anonymous compatibility endpoint was introduced.

## Known Limitations

- Final clean-browser human verification is still human-owned.
- Authorized admin read positive path is still pending.
- Custom-domain HTTPS certificate is still pending on Cloudflare Pages.
- Beta smoke against `https://app.lythaus.asora.co.za` is pending until HTTPS is active.
