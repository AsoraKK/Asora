# PostgreSQL Schema Inventory

## Status

**BLOCKED — no PostgreSQL client tools or approved database credentials were available in the audit workstation.**

Known live server metadata:

- Server: `asora-pg-dev-ne`
- PostgreSQL: 16
- SKU: `Standard_B1ms`
- Storage: 32 GB
- Backup retention: 7 days
- HA: Disabled
- Public access: Enabled

Unverified: database names, schemas, tables, views, sequences, extensions, roles, indexes, constraints, row counts, UUID columns, identity/provider mappings, DSR records, Entra access, firewall rules, and SSL enforcement.

No SQL records were queried and no dump was created.
