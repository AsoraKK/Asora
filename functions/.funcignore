# Minimal allowlist for Flex (OneDeploy): include only runtime assets

# Ignore everything by default
*

# Allow required runtime files
!host.json
!package.json
!dist/**
!node_modules/**
!index.js

# Always exclude local-only settings
local.settings.json
.env
.env.*
